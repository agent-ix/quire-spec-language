{
  "repository": "https://github.com/agent-ix/spec-artifacts-iso",
  "revision": "6686f11",
  "revision_full": "6686f112f2c38602c9d39c88e8134a945c34bbd6",
  "module": "edge-vocabulary",
  "module_version": "0.2.0",
  "paths": [
    {
      "source": "spec_artifacts_iso/manifest.yaml",
      "source_lines": "872-975",
      "source_lines_sha256": "8d778a8e8d2e59124e2ce4b159e47aa20626e85798d91bf8c680a6199ba38fce",
      "fixture": "manifest.yaml",
      "edits": [
        "lines 872-975 (the `edge_types:` and `roles:` registries) copied byte for byte; every other line of the ISO manifest (artifact types, grammars, lint rules, traceability, the ISO semantic block) dropped",
        "authored header: manifest_version, name edge-vocabulary, version 0.2.0 (the ISO module's version), description, an empty semantic block (exports: []) so Bundle::load accepts the module, and empty archetypes/grammars/artifact_types/lint_rules/object_types lists"
      ]
    }
  ],
  "edits": [],
  "note": "spec-objects-business d1840b8 declares allowed_links verbs (contains, aggregates, composes, references, owns, ...) but no edge_types registry: the EdgeTypeDef {category, inverse} FR-094 reads lives in spec-artifacts-iso's manifest, which the frontend cannot vendor whole (58 schema files) for one registry. This module carries that registry alone and is loaded beside spec-objects-business by every test that lowers a frontmatter edge; a bundle lifted under spec-objects-business alone raises UNKNOWN_EDGE_VERB for every allowed verb, which is FR-094's rule and not a fixture defect (reported with Task-133)."
}
