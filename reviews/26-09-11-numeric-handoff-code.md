---
id: SR-380
title: "Code review of full-domain numeric producer handoff"
type: SpecReview
analysis: code-review
scope: "examples/native_protocol_handoff.rs; examples/protocol-handoff/; tests/native_numeric_domain_emission.rs; tests/support/composed_types/mod.rs; tests/support/native_protocol/mod.rs"
review_set: subset
relationships:
  - target: ix://agent-ix/quire-spec-language/FR-042
    type: reviews
  - target: ix://agent-ix/quire-spec-language/TC-121
    type: references
---

## Summary

Code/Rust review examined the producer example and its domain fixture against
bb30eac. Wide and Exact retain full signed-64 numerator/integer bounds and the
admitted maximum denominator. Workflow fields reach those types through the
Healthy invariant's call to Bounded; they are not unused inventory entries.
No production compiler, wire contract, dependency pin or hard limit changes.

## Verdict

**PASS** for this producer change — endpoint execution, the real release producer
and both strict Clippy configurations pass. Actual B consumer acceptance remains
separate from this producer-side verification.

## Findings

| ID | Severity | Summary | Refs |
| --- | --- | --- | --- |
| FND-001 | low | No unresolved findings in the reviewed producer change. | - |

## Evidence and limits

Two review findings were resolved: the fixture now reaches the actual maximum
denominator rather than calling i64::MAX-1 the ceiling, and new dead-code
allowances name the independently compiled shared-fixture constructor rationale.

The emitted Bounded declaration contains exact integer values i64::MIN and
i64::MAX, and reduced rational pairs (i64::MAX, i64::MAX-1) and
(i64::MIN, i64::MAX). Tests compare decoded components and the tagged decimal
spellings, and the independently admitted package retains the same values.
The existing maximum-denominator check in pinned Contract IR rejects only zero
or values greater than i64::MAX; no producer bound or refusal was relaxed.

The actual stripped release test passes with original source/declaration
ownership, full numeric type domains and both compensation obligations. The
release example regenerated compiled-protocol.json, expected.json and the
external seal in a fresh output directory. Independent SHA-256 measurement of
the emitted bytes matches that generated seal. No generated selector or seal
was hand-edited. Export indices measured from the generated model inventory are
Exact 16, Tally 19, Total 20 and Wide 21; B must re-derive its selections.

Baseline worker logs on the equivalent df2f9a8 tree record minimal/all-feature
full suites of 606/622 passed, zero failures and four inherited ignored each.
After the endpoint correction, this review reran the numeric integration test
and the actual ignored stripped-release producer test: both pass. Formatting
passes. Both Clippy configurations pass on the final change with warnings denied.

No applicable AssuranceProfile, new runtime API, unowned production behavior,
unsafe code, new dependency or hosted-workflow change was found. The shared
fixture's JSON construction and test-only indexing follow the existing fixture
conventions; no second semantic frontend was introduced.

No plan bundle covers this stage. Issue #66 explicitly retains issue-based
planning, so the gap-analysis plan-completion gate is not claimed. FR-042-AC-10
still requires B's actual consumer to accept the regenerated bytes and retain
the independently selected identities. Remaining work: #40.
