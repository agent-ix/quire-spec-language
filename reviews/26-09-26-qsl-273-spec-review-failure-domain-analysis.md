---
id: SR-664
title: "QSL-273 failure-domain review of state clauses on the spine"
type: SpecReview
analysis: failure-domain
scope: "agent-ix/quire-spec-language@d8b74aba7d349ccb3989583cc4e608aad301c38b; spec/functional/FR-104, FR-106, FR-107, FR-109; spec/decisions/ADR-012-semantic-family-extension-contracts.md (§15.1, §15.5, §15.7); spec/decisions/ADR-014 (§4, unchanged); spec/test-cases/TC-466; quire-specification@0d53cf2 proposals/quire-v1/state-contract.md; code qsl-semantics/src/check/facts.rs, qsl-eval/src/value/expression/s6a.rs"
review_set: subset
relationships:
  - target: ix://agent-ix/quire-spec-language/FR-104
    type: reviews
  - target: ix://agent-ix/quire-spec-language/FR-106
    type: reviews
  - target: ix://agent-ix/quire-spec-language/FR-107
    type: reviews
---

## Summary

Ticket: QSL-273 (PR agent-ix/quire-spec-language#462). This review looks for
unstated failure modes, identity confusion across observations, and places
where the spec departs from the QSpec state contract (`state-contract.md`,
"Operation anchors, aliases and captures" and "Finite graph extension").

Clean: `reaches` follows the contract. It needs one or more steps, tests
before it suppresses a repeat, and expands each identity once, so an isolated
`a` does not reach itself. Meter exhaustion is `Incomplete`, never `false`.
The `pre(result)` and `pre(parameter)` refusals match the contract table.
Admission reads no ambient state.

Verdict: changes requested (five medium).

## Findings

| ID | Severity | Summary | Refs |
| --- | --- | --- | --- |
| FND-001 | medium | Edge and value shapes go past the QSpec extension. The state contract admits a `reaches` edge only of type `Ref(T)`, `Option(Ref(T))` or `Seq(Ref(T),N)`, and says "Sets, bags, OrderedSet ... remain outside this selected extension". FR-104 admits "a collection of `Reference<M::T>`". FR-107 follows edges "in declared order", which a set or bag does not have. FR-106's snapshot values take set, bag and ordered-set kinds. Failure scenario: `reaches` over a `Set<Ref(T)>` edge checks. Two evaluators expand the set in different orders, charge differently, and hit meter exhaustion at different budgets, so one input gives `Incomplete` in one and `Completed` in the other. Fix: restrict the edge to sequence and option, and refuse set, bag and ordered-set values at admission, or ask QSpec for the extension. | spec/functional/FR-104-check-state-clauses.md:97-101; spec/functional/FR-107-evaluate-state-clauses-at-s6a.md:77-83; spec/functional/FR-106-admit-snapshots-and-invocations.md:96-101 |
| FND-002 | medium | The invariant anchor departs from the QSpec state contract. The contract sets an invariant's `self` to the "exact named initialization/handler observation's current snapshot" and says "a bare context type or arbitrary current snapshot is not a substitute". FR-106's `Current { snapshot, self }` accepts any current snapshot, and ADR-012 §15.1 treats that as in scope. Native-run/1 does the same, so this is parity. The departure is still not recorded. Failure scenario: a CG or RT consumer built to the QSpec contract refuses the same invariant selection that QSL admits, and the two disagree on the ConfigVersion corpus. Fix: record it as a QSpec delta on the QSpec ticket, or name the binding that makes a snapshot the invariant's observation. | spec/functional/FR-106-admit-snapshots-and-invocations.md:125-128, 148-153; spec/decisions/ADR-012-semantic-family-extension-contracts.md:1113-1133 |
| FND-003 | medium | Guard facts can cross observations. The contract refuses `present(pre(self.parent)) implies value(self.parent)` ("cross-observation presence fact"), and refuses `let v = self.version in pre(v)` and `let s = self in pre(s.version)`. FR-104 states none of these and has no AC for them. It relies on the "definedness cause it has today", and today no presence fact is derived for these paths at all (SR-660 FND-001). The obvious fix for SR-660 FND-001 is to add `Attribute` stable paths. Keyed without the observation, that admits the cross-observation guard. Failure scenario: `post P ... { present(pre(self.parent)) implies deref(value(self.parent)).versionNumber > 0 }` checks. On an invocation whose post sets `child.parent` absent (inside the frame for an operation that modifies `parent`), S6a reaches `value` on an absent post field. The result is a truth-less `Undefined`, where the contract requires a check-time refusal. Fix: key presence and interval facts by observation, apply the QSL-228 captured-alias rule to model reads, and add the three contract rows as FR-104 adverse ACs. | spec/functional/FR-104-check-state-clauses.md:91-96, 104-107 |
| FND-004 | medium | The extent is `Bounded` for clauses that range over an unbounded object universe. FR-104 and ADR-012 §15.7 compute the extent over the types of `self`, `result` and the parameters, and conclude that "a clause over bounded fields is `Bounded`". An invariant holds for every object of `T`, and `reaches` walks as far as the population allows. ADR-014 §4 lists "population with `Population(None)`" as an unbounded domain, and `config_history` declares no maximum. Failure scenario: once IR lowers `state` nodes, a bounded-only backend is told `NoCycle` is `Bounded`, proves it over a small fixed heap, and settles it `supported` with no bound requested. Fix: treat the clause's context population (and the traversal's population) as a `Cardinality`-boundable domain, or record it as an ADR-014 open question and mark the extent provisional. | spec/functional/FR-104-check-state-clauses.md:117-120; spec/decisions/ADR-012-semantic-family-extension-contracts.md:1230-1239 |
| FND-005 | medium | `reaches` in a function body has no evaluator. FR-104 admits `reaches` "in a state clause or a function body", and TC-466 step 3 calls `function r ... { reaches(x, y, parent) }` through `call`. FR-102 gives `Reaches` to `StateModel`, FR-107 adds only a `ProtocolClause` S6a arm (the one existing arm is `Value`, qsl-eval/src/value/expression/s6a.rs:93-96), and FR-107 defines `reaches` only under "the observation in force" of a clause. Failure scenario: TC-466 step 3's `call` reaches a `Reaches` node that the `Value` evaluator has no arm for. The result is `InternalFault`, or an ad hoc traversal whose charging differs from the clause path, so the step 3 meter sweep gives two thresholds for one query. Fix: say which evaluator runs `Reaches` in a function body over a single `ObjectEnvironment`, with the same order and charging, or refuse `reaches` outside a state clause. | spec/functional/FR-104-check-state-clauses.md:97; spec/functional/FR-102-build-state-clause-forms.md:79-81; spec/test-cases/TC-466-s6a-evaluates-state-clauses.md:33-40 |
| FND-006 | low | Integer spellings are not canonical. Values are decimal strings, but FR-106 does not say whether `"01"`, `"+1"` or `"-0"` are admitted. Failure scenario: two snapshots with equal content and different spellings admit to equal values under different `sha256-jcs` digests, so a replay pinned to one digest refuses the other. Fix: use FR-038's integer spelling and refuse any other with `invalid-value`. | spec/functional/FR-106-admit-snapshots-and-invocations.md:68-69, 96-98 |
| FND-007 | low | A label mismatch is misreported as a digest mismatch. FR-106 check 1 sends "four non-blank labels equal to the selection's" failures to `stale_dependency`/`byte-digest-mismatch`, even when the bytes match their digest. Failure scenario: a caller whose document is intact but whose selection names another revision is told the bytes are corrupt. `revision-mismatch` is the catalog cause for a label disagreement. | spec/functional/FR-106-admit-snapshots-and-invocations.md:137-146 |
| FND-008 | medium | Disposition pass, at c35a6a49. The new population domain has no key. FR-104 and ADR-012 §15.7 make each clause `Unbounded` over its context's population as a `Population(None)` domain. ADR-014 §4 keys every unbounded domain by `DomainKey{node: WireNodeId, path}`, and FR-094 gives a population record no node. Neither FR-104 nor §15.7 says which node and path key the `config_history` domain (the `self` parameter node? a `relation`/`population` node?). "The context's population" is also ambiguous when the context type belongs to several populations, which TC-465 row 25's `archive` fixture now creates. Failure scenario: the O-20 request writer cannot build the `ProofBound` map key for FR-104-AC-5's domain, or two implementations key it differently and a bounded request built for one does not match the other. Fix: name the key (for example the clause's `self` parameter node with an empty path) and the population chosen when the type has several. | spec/functional/FR-104-check-state-clauses.md:163-170, 178; spec/decisions/ADR-012-semantic-family-extension-contracts.md:1269-1281 |
| FND-009 | low | Disposition pass, at c35a6a49. The observation of a reference-typed operation parameter is unspecified. FR-104 gives each read the observation of the reference it goes through, and it gives `self` and references read inside an observation their observation, but a parameter reference is not read in any observation. QSpec says "A pre selector cannot retag a post-qualified parameter reference" and "The model/binding must supply any reference qualification". TC-466 step 3's `probe(target)` is a precondition, so it never exercises the question. Failure scenario: in a postcondition, `deref(target).versionNumber` reads post in one implementation and pre in another, and `pre(deref(target).f)` is admitted by one and refused by the other. Fix: state the qualification a parameter reference gets (for example `post` in a postcondition, not retaggable by `pre`) and add an AC. | spec/functional/FR-104-check-state-clauses.md:106-118 |
| FND-010 | low | Disposition pass, at c35a6a49. FR-106 check 11's violation order omits a case the frame check it reuses reports, and it differs from that check's structure. `enforce_frame` refuses a surviving object whose most-specific type changed (`FrameTypeChanged`, qsl-semantics/src/model/population.rs:1432) and checks creates before deletes. `admit_invocation` also admits one population role per call, while FR-106 admits multi-population snapshots. Failure scenario: an invocation whose post changes `child`'s type and deletes `root` reports either violation, depending on whether the implementer adds the type change before or after 11.1. Fix: place the type change in the order, and say that the frame check runs per population, in document order. | spec/functional/FR-106-admit-snapshots-and-invocations.md:232-250 |
| FND-011 | medium | Round 2, at 39647e7c. FR-104 gives every reference-typed operation parameter the `pre` observation, in a postcondition too, and quotes QSpec's "A pre selector cannot retag a post-qualified parameter reference" as its support. That sentence only makes sense if a parameter reference in a postcondition is post-qualified, which is the opposite of the rule. Under the rule, a postcondition cannot read the post state of any object reached through a parameter: `deref(target).versionNumber` in `post` reads `pre`. Failure scenario: an author writes `post P ... on M::T::transfer { deref(to).balance = pre(deref(to).balance) + amount }`. The left read silently reads pre, so the clause checks the wrong state, while the right side refuses `forbidden-pre-read`. A post-state claim about a parameter object has no spelling. Fix: qualify a parameter reference with the clause's own observation (`pre` in a precondition, `post` in a postcondition), with `pre(e)` retagging a read through it, as it retags `self`. Or record the pre-only reading as a QSpec question on STD-111, and change FR-104-AC-8. | spec/functional/FR-104-check-state-clauses.md:117-125, 210 |
| FND-012 | low | Round 2, at 39647e7c. FR-106 check 11 says its within-population order "is `enforce_frame`'s", but step 3's "fields in declared order" is not. `enforce_frame` collects each survivor's fields into a `BTreeSet<&DeclarationKey>` and walks them in ascending key order (qsl-semantics/src/model/population.rs:1488-1490), not the type's declared order. Steps 1, 2 and 4 do match (BTreeMap walks by object key; duplicate, then overlap, then mismatch). The text also does not say what `admit_invocation` receives for a population present in only one snapshot (an empty document for the other side is the reading that yields creates and deletes). Failure scenario: an invocation changing two fields outside the frame gets a different named field from an implementation that follows the text than from one that reuses `enforce_frame`. Fix: pick one order and name it, and state the empty-document rule. | spec/functional/FR-106-admit-snapshots-and-invocations.md:238-259 |
| FND-013 | low | Round 2, at 39647e7c. "Ascending declaration-identity order", which fixes a population domain's ordinal, is not defined. It could be the lexical byte order of the IR node identity string, or `DeclarationKey`'s `Ord` over (package, node). The two agree within one package only if `package` is constant, which the text doesn't state. Failure scenario: two implementations order `config_history` and `archive` differently and write different `DomainKey` paths for one package. Fix: name the comparator, for example the byte order of the population's IR node identity. (The ordinal key itself is accepted by the leader. It is not stable when a population is added: a new population whose identity sorts first shifts every later ordinal, although the object type node ids stay the same under one (identity, version). This only matters if something compares domain keys across two domain-package digests.) | spec/functional/FR-104-check-state-clauses.md:180-194 |

## Verdict

Changes requested. FND-003 and FND-005 cause truth-less or faulting results
at run time. FND-001 and FND-002 are undeclared departures from the QSpec
contract.

## Dispositions

Disposition pass at `agent-ix/quire-spec-language@c35a6a49` (fix commit `c35a6a49`, "QSL-273 spec: fix SR-660 to SR-664 review findings", rebased onto main 5e7a2615). Each outcome was re-checked against the spec and code at that head, not taken from the commit message. `quire validate` over the changed spec files and these reviews exits 0 with no EARS warnings.

| FND | Outcome | sha/reason |
| --- | --- | --- |
| FND-001 | fixed c35a6a49 | Edges are reference, option or sequence only; snapshot values have no set, bag or ordered-set form, and a set-typed field refuses at admission (FR-106 6.2, TC-465 row 17). |
| FND-002 | fixed c35a6a49 | Snapshots and `Current` selections carry an `anchor` (`initialization` or `handler`), checked at admission (check 3, TC-465 row 12). |
| FND-003 | fixed c35a6a49 | Facts are keyed by (observation, path); FR-104 states the `let` and `pre` capture rules, and AC-7 adds QSpec's three rows. |
| FND-004 | fixed c35a6a49 | The context population and each walked population are `Population(None)` domains, so ConfigVersion's clauses are `Unbounded` (FR-104-AC-5). The domain's key is unspecified: new FND-008. |
| FND-005 | fixed c35a6a49 | `reaches` outside a state clause refuses `ill_typed`/`operator-ineligible` (FR-104-AC-4), and TC-466 step 3 now uses a precondition. |
| FND-006 | fixed c35a6a49 | Integers use FR-038's spelling, and `"01"` refuses `invalid-value` (TC-465 row 19). |
| FND-007 | fixed c35a6a49 | A label mismatch refuses `stale_dependency`/`revision-mismatch` (check 1.8, TC-465 row 9). |

### Round 2

Disposition pass at `agent-ix/quire-spec-language@39647e7c` (fix commit `f83ca481`, "QSL-273 spec: fix disposition-pass findings (SR-660, SR-662, SR-664)"). Each outcome was re-checked against the spec and code at that head. `quire validate` over the changed spec files and the reviews exits 0.

| FND | Outcome | sha/reason |
| --- | --- | --- |
| FND-008 | fixed f83ca481 | The population domain key is `DomainKey{node: member object_type node, path: [population ordinal]}`. A bounded population adds no domain, and two unbounded populations of the context type refuse `ambiguous_declaration` at `on` (FR-104-AC-5, TC-461 step 5). The comparator behind the ordinal is new FND-013. |
| FND-009 | fixed f83ca481 | FR-104 now states a qualification for parameter references (pre, not retaggable) with AC-8 and TC-459 step 5. The rule chosen conflicts with QSpec's wording: new FND-011. |
| FND-010 | fixed f83ca481 | Check 11 runs per population, includes `FrameTypeChanged`, and follows `enforce_frame` for creates/type change, deletes and the delta (TC-465 rows 39 and 40). The field order within step 3 differs from the code: new FND-012. |
| FND-011 | fixed a7ba0777 | FR-104 now gives a read through a reference-typed parameter the clause's own observation (`pre` in a precondition, `post` in a postcondition), and `pre(e)` retags it as it does `self`. Only `pre(target)` refuses `forbidden-pre-read`, citing QSpec's "A pre selector cannot retag a post-qualified parameter reference" and "a bare or parameter-only historical selection refuses". FR-104-AC-8 and TC-459 step 5 (a) to (d) cover it. |
| FND-012 | fixed a7ba0777 | Check 11 step 3 walks fields in ascending `DeclarationKey` order (population.rs:1488-1490). A population in only one snapshot is admitted against an empty document for the other side (FR-106-AC-5, TC-465 row 41). |
| FND-013 | fixed a7ba0777 | The ordinal uses `DeclarationKey`'s own `Ord` (package, then node, as UTF-8 bytes, key.rs:80-84). FR-104 and ADR-012 §15.7 state that it is stable within one domain-package digest only and that domain keys are not compared across digests. |
