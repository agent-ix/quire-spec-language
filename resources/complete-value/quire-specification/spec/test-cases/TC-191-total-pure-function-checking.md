---
id: TC-191
title: "Total pure function checking"
type: TC
relationships:
  - target: ix://agent-ix/quire-specification/FR-146
    type: verifies
---

# TC-191: Total pure function checking

## Description

Check typed pure calls, proved recursion, failed termination and prohibited effects.

## Test Procedure

Use the exact version-locked subject and inputs named by FR-146. Execute its positive case, each declared boundary, and each explicit refusal/non-conclusive mutation through the real local Rust boundary; compare the complete typed result and artifact accounting with the criterion.

Select the `quire.value.complete/v1` definitions at revision `1-draft.1` by
their exact DefinitionRefs in
[`complete-value-lock.json`](../../proposals/quire-v1/definitions/complete-value-lock.json).
Unless a row states limits, run it under limits large enough that no charge is
denied. Integer literals, parameters, `let` names and field projections make no
charge. Every `ill-typed` expectation is a type-checking
`refused { code: ill_typed }` made before any charge.

| Vector | Input | Expected result/disposition |
| --- | --- | --- |
| P01 | `record Node { head: Integer; next: Option<Node>; }` and `function last using V(n: Node): Integer pure decreases(n) { if present(n.next) then last(value(n.next)) else n.head }` | admitted: the recursive argument `value(n.next)` is a projection path of the parameter whose `value` step is under the proved `present(n.next)` guard fact |
| P02 | `function loop using V(n: Int[0,9]): Integer pure decreases(n) { loop(n) }`; the same without `decreases` | `refused { code: undefined_expression, cause: unproved-decrease }` naming cycle `loop -> loop`, edge `loop(n)` and obligation `decrease` (`n < n`); the same refusal with obligation `missing-measure` |
| P03 | `function even using V(n: Int[0,9]): Boolean pure decreases(n) { if n = 0 then true else odd(n - 1) }` with `odd` symmetric; then `odd` declaring `decreases(M2(n, 0))` for `tuple M2(Integer, Integer);` | admitted; `refused { code: undefined_expression, cause: unproved-decrease }` naming the component `even -> odd -> even` and obligation `measure-arity` |
| P04 | With `type Total = Integer;`: `function q using V(a: Int[-9,9], b: Int[-9,9]): Rational[-9,9;1,9] pure { a / b }`; the same body as `if b != 0 then a / b else rational(0, 1)`; a body `value(o)` for parameter `o: Option<Integer>` without `present(o)`; a body `reduce<Total>(acc, x in s: acc + x)` for parameter `s` of `Sequence<Integer>[0,3]` and of `Sequence<Integer>[1,3]`; the `[0,3]` body as `if size(s) >= 1 then reduce<Total>(acc, x in s: acc + x) else 0` | linking returns `refused { code: undefined_expression, cause: unproved-nonzero }` at the division; admitted; `refused { code: undefined_expression, cause: unproved-presence }` at `value`; `refused { code: undefined_expression, cause: unproved-range }` at the `reduce` for minimum 0, naming `size(s) >= 1`; admitted for minimum 1; admitted under the guard fact. TC-190 Q04 is the unlinked kernel-evaluation context of the same `reduce` |
| P05 | `function r using V(x: Integer): Integer pure { if true then 1 else undeclaredHost(x) }`; then the same body calling `M::pay(x)`, where `M::pay` is an operation of imported model `M` | `refused { code: missing_declaration, cause: missing-name }` at `undeclaredHost` although the branch is unreachable, with no call edge; `refused { code: ill_typed, cause: operator-ineligible }` at `M::pay`, also although unreachable |
| P06 | `last` from P01 applied to a three-node chain; then under `ScalarLimitsV1` with every counter unlimited except `work_units: 2` | the third node's head after exactly three `function.call` charges; `incomplete { limit_kind: work_units, limit: 2, consumed: 2, next_charge: 1, charge_point: function.call }` for the third call, and `last` remains admitted |
| P07 | `function double using V(n: Int[0,9]): Integer pure { n + n }` and `function quad using V(n: Int[0,9]): Integer pure { double(double(n)) }`, neither declaring `decreases`; then `double` declaring `decreases(n)` | both admitted, because neither is in a recursive component; `double` is still admitted, with its measure type checked and no obligation |
| P08 | Measures, with `tuple P2(Integer, Integer);`: (1) `function down using V(n: Int[0,9]): Integer pure decreases(n) { if n > 0 then down(n - 1) else 0 }`; (2) the same with `if n > 1 then down(n - 2) else 0`; (3) (1) with `decreases(n + 0)`; (4) (1) with `n: Integer`; (5) `function two using V(n: Int[0,9], m: Int[0,9]): Integer pure decreases(n) { if m > 0 then two(m - 1, m - 1) else 0 }`; (6) `function g using V(s: Sequence<Integer>[0,3]): Integer pure decreases(size(s)) { if size(s) > 0 then g(s) else 0 }`; (7) `function lex using V(a: Int[0,9], b: Int[0,9]): Integer pure decreases(P2(a, b)) { if b > 0 then lex(a, b - 1) else if a > 0 then lex(a - 1, 9) else 0 }` | (1) admitted: `n > 0` refines `n` to `[1,9]`, so `n - 1` lies in `[0,8]`; (2) admitted; (3) `refused { code: undefined_expression, cause: unproved-decrease }` with obligation `measure-kind`; (4) the same refusal with obligation `nonnegative`; (5) the same refusal with obligation `decrease`, because `m - 1` is not `n - k`; (6) the same refusal with obligation `decrease`, because no form proves a smaller cardinality; (7) admitted: the first edge keeps `a` equal and decreases `b`, and the second decreases `a` |
| P09 | Intervals, with parameters `a` and `b` of one type `Int[0,3]` and result `Int[0,9]`: bodies `a * b`; `a * b + 1`; `if a < 3 then a * b + 1 else 0`; `if a < b then b - a else 0`; `-a + 3`. Then, with P04's `q`, the bodies `if b > 0 then a / b else rational(0, 1)` and `if b >= a then a / b else rational(0, 1)` | admitted (`[0,9]`); `refused { code: undefined_expression, cause: unproved-range }`, because `[1,10]` exceeds `[0,9]`; admitted, because `a < 3` refines `a` to `[0,2]`; `unproved-range`, because a comparison of two non-literal operands yields no fact and `b - a` is `[-3,3]`; admitted (`[0,3]`); admitted, because `b` is refined to `[1,9]`; `unproved-nonzero` |
| P10 | With `record Box { o: Option<Integer>; }`, `function pick using V(x: Box): Option<Integer> pure { x.o }`, parameter `f: Float64`, parameter `ro: Option<Reference<M::Obj>>`: bodies `if present(x.o) then value(x.o) else 0`; `if present(pick(x)) then value(pick(x)) else 0`; `let y = pick(x) in if present(y) then value(y) else 0`; `convert<Rational[-9,9;1,9]>(f)`; `if present(ro) then deref(value(ro)).n else 0` for an `Integer` attribute `n`; `deref(ro).n`. Then two `function dup using V(): Integer pure { 1 }` declarations; then a checker that declares an NFR-010 node admission limit below the node count of `down` from P08 | admitted; `refused { code: undefined_expression, cause: unproved-presence }`, because `pick(x)` is not a stable path; admitted; admitted with no body obligation; evaluated with `f` NaN or infinite it is undefined after `ieee.operands`, with `f` = 20.0 it is `refused { code: ieee_rational_out_of_domain }`, and with `f` = 0.5 it returns `1/2`; admitted, with target existence recorded as a runtime input requirement; `refused { code: ill_typed, cause: type-mismatch }`; each `dup` is `refused { code: ambiguous_declaration, cause: ambiguous-name }` listing both loci; `resource_exhausted` with cause `insufficient-next-charge` naming the checking stage and the declared limit, and no admission verdict for `down` |
| P11 | Evaluation charges under `quire.value.accounting/v1`: `down(2)` for P08's admitted `down`; then under `ScalarLimitsV1` with every counter unlimited except `work_units: 17`; `q(3, 2)` for P04's guarded `q`; then with only `work_units: 9`; then `a > 0 implies b > 0` for `a = 3`, `b = 2` and for `a = -1`, `b = 2` | `0` after `function.call`, `n > 0` (`ordering.operands` with `integer_bits` 2 and `value_occurrences` 2, `ordering.arithmetic`, `ordering.result-retain`), `n - 1` (three `integer-arithmetic.*` charges), `function.call`, `1 > 0`, `1 - 1`, `function.call` and `0 > 0`: eighteen work units and five result units; `incomplete { limit_kind: work_units, limit: 17, consumed: 17, next_charge: 1, charge_point: ordering.result-retain }` at `0 > 0`; `3/2` after `function.call`, the five-unit `b != 0` equality, then `rational-arithmetic.operands` (`integer_bits` 2), `rational-arithmetic.arithmetic` (`N = 3`, `D = 2`, `integer_bits` 2), `rational-arithmetic.normalize` (`integer_bits` 2) and `rational-arithmetic.result-retain`: ten work units and two result units; `incomplete { limit_kind: work_units, limit: 9, consumed: 9, next_charge: 1, charge_point: rational-arithmetic.result-retain }`; true after two orderings and `boolean.result-retain`: seven work units and three result units; true after one ordering and `boolean.result-retain`, with the right operand skipped: four work units and two result units |

## Expected Results

Every positive and boundary result matches FR-146; each invalid, unsupported or exhausted mutation returns its exact typed disposition with source/provenance, emits no approximation, and preserves independent sibling results.
