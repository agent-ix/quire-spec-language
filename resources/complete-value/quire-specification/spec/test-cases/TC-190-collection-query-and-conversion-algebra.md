---
id: TC-190
title: "Collection query and conversion algebra"
type: TC
relationships:
  - target: ix://agent-ix/quire-specification/FR-145
    type: verifies
---

# TC-190: Collection query and conversion algebra

## Description

Run map/filter/flatten/fold and explicit lossy conversions across every collection kind.

## Test Procedure

Use the exact version-locked subject and inputs named by FR-145. Execute its positive case, each declared boundary, and each explicit refusal/non-conclusive mutation through the real local Rust boundary; compare the complete typed result and artifact accounting with the criterion.

Select the `quire.value.complete/v1` definitions at revision `1-draft.1` by
their exact DefinitionRefs in
[`complete-value-lock.json`](../../proposals/quire-v1/definitions/complete-value-lock.json).
Unless a row states limits, run it under limits large enough that no charge is
denied. Integer literals, parameters, `let` names and field projections make no
charge. Every `ill-typed` expectation is a type-checking
`refused { code: ill_typed }` made before any charge.

| Vector | Input | Expected result/disposition |
| --- | --- | --- |
| Q01 | `map(x in c: 0)` for parameter `c` of `Sequence<Integer>[0,2]` holding `1, 2`, of `Set<Integer>[1,2]` holding `1, 2`, of `Bag<Integer>[0,2]` holding `1, 2` and of `OrderedSet<Integer>[0,2]` holding `2, 1` | `Sequence<Integer>[0,2]` `[0, 0]`; `Set<Integer>[1,2]` `{0}`; `Bag<Integer>[0,2]` with `0` of multiplicity 2; `OrderedSet<Integer>[0,2]` `[0]` |
| Q02 | `filter(x in b: x = 1)` for parameter `b` of `Bag<Integer>[1,3]` holding `1, 1, 2`; `filter(x in o: x != 1)` for parameter `o` of `OrderedSet<Integer>[1,3]` holding `2, 1, 3` | `Bag<Integer>[0,3]` with `1` of multiplicity 2; `OrderedSet<Integer>[0,3]` `[2, 3]` |
| Q03 | `flatten(c)` for parameter `c` of `Sequence<Sequence<Integer>[0,2]>[0,2]` holding `[1, 2]` and `[2]`, of `Set<Set<Integer>[0,2]>[0,2]` holding `{1}` and `{1, 2}`, of `Bag<Bag<Integer>[0,2]>[0,2]` holding `1` and `1, 2`, and of `OrderedSet<OrderedSet<Integer>[0,2]>[0,2]` holding `[2, 1]` and `[1, 3]`; a `Set<Set<Integer>[1,2]>[1,3]`; then `Sequence<Set<Integer>[0,2]>[0,2]`, `OrderedSet<Bag<Integer>[0,2]>[0,2]` and `Set<Sequence<Integer>[0,2]>[0,2]` | `[1, 2, 2]`; `{1, 2}`; `1` with multiplicity 2 and `2` with multiplicity 1; `[2, 1, 3]`; result type `Set<Integer>[1,6]`; `refused { code: ill_typed, cause: type-mismatch }`; the same refusal; admitted with result kind set |
| Q04 | With `type Total = Integer;`, direct kernel evaluation (no linking) of `fold<Total>(acc, x in e: acc + x, identity: 0)` and `reduce<Total>(acc, x in e: acc + x)` for empty parameter `e` of each of `Sequence<Integer>[0,2]`, `Set<Integer>[0,2]`, `Bag<Integer>[0,2]` and `OrderedSet<Integer>[0,2]`; then linking `function r using V(e: Set<Integer>[0,2]): Integer pure { reduce<Total>(acc, x in e: acc + x) }`; then `fold` without `identity:` and `reduce` with `identity: 0` | `0` for each fold, after exactly one accumulator retain (one work unit and one result unit); a located undefined outcome for each of the four reductions, with no charge; `refused { code: undefined_expression, cause: unproved-range }` at the `reduce`, naming `size(e) >= 1`, as in TC-191 P04; `refused { code: ill_typed, cause: type-mismatch }`; the same refusal |
| Q05 | With `type Total = Integer;`, `type Small = Int[0,30];` and `type Flag = Boolean;`, so `A` is `Total`, `Small` or `Flag`: over `s` of `Set<Integer>[0,3]` holding `{1, 2, 3}`, `fold<Total>(acc, x in s: t, identity: 0)` for steps `t` = `acc + x`, `x * acc`, `acc - x`, `acc + acc`, `add(acc, x)` for `function add using V(a: Integer, b: Integer): Integer pure { a + b }`, `x = 1`, and `acc + x` with `identity: true`; over `Set<Small>[0,3]`, `fold<Small>(acc, x in s: acc + x, identity: 0)`, whose step is well typed; over `Set<Int[0,10]>[0,3]`, the same `fold<Small>`; over `Set<Boolean>[0,2]`, `fold<Flag>(acc, x in s: acc and x, identity: true)`; over `Sequence<Integer>[0,3]`, `fold<Total>(acc, x in s: acc - x, identity: 0)` | admitted with fold value 6; admitted; `refused { code: ill_typed, cause: operator-ineligible }`; the same refusal (`acc` occurs in `t`); the same refusal, even though `add` is commutative and associative; `refused { code: ill_typed, cause: type-mismatch }`, because the step must have type `A`; the same type-mismatch refusal for the identity; `operator-ineligible` (bounded accumulator, found by the catalog after typing succeeds); the same `operator-ineligible` refusal, because the step is well typed and the catalog's condition that `t` has type `A` fails for `x` of `Int[0,10]`; admitted; admitted |
| Q06 | For parameters: `convert<Set<Integer>[0,3]>(a)` with `a` of `Sequence<Integer>[0,3]` holding `1, 2, 3`; `convert<Sequence<Integer>[0,3]>(b)` with `b` of `Bag<Integer>[0,3]` holding `2, 1, 1`; `convert<Bag<Integer>[0,2]>(o)` with `o` of `OrderedSet<Integer>[0,2]` holding `2, 1`; `convert<Set<Integer>[0,1]>(d)` with `d` of `Sequence<Integer>[0,2]` holding `1, 1`; `convert<Set<Integer>[0,1]>(e)` with `e` of `Sequence<Integer>[0,2]` holding `1, 2`; `convert<Set<Int[0,5]>[0,2]>(x)` for `x` of `Set<Integer>[0,2]` | `{1, 2, 3}` with `CollectionLoss { discarded: [order, multiplicity] }` although no duplicate occurred; `[1, 1, 2]` with `discarded: []`; `discarded: [order, uniqueness]`; `{1}`, checked after coalescing; `refused { code: cardinality_out_of_bound, cause: above-maximum }`; `refused { code: ill_typed, cause: type-mismatch }` for the element conversion |
| Q07 | `map(x in s: 0)` for parameter `s` of `Set<Integer>[0,3]` holding `1, 2, 3`; then with `ScalarLimitsV1` with every counter unlimited except `work_units: 10` | `{0}` after three `collection.visit`; for the second and third results, each `collection.member-walk` (2) and `collection.member-test` (`p = 1`) against `0`, equal, coalesced; `collection.bound` (`value_occurrences = 1`); and a retain of `occ = 2`: eleven work units and two result units; then `incomplete { limit_kind: work_units, limit: 10, consumed: 10, next_charge: 1, charge_point: collection.result-retain }` |
| Q08 | `exists(x in q: x = 2)` for parameter `q` of `Sequence<Integer>[0,3]` holding `1, 2, 3`; then `filter(x in q: x = 1)` under `ScalarLimitsV1` with every counter unlimited except `work_units: 7` | true after visit, one-pair equality (`equality.plan-form` 2, `equality.plan`, one pair, retain: five work units), visit, one-pair equality, then a scalar retain, with no visit of `3`: thirteen work units and three result units; `incomplete { limit_kind: work_units, limit: 7, consumed: 7, next_charge: 2, charge_point: equality.plan-form }` at the second predicate, with no partial collection |
| Q09 | `convert<Set<Integer>[0,3]>(q)` for parameter `q` of `Sequence<Integer>[0,3]` holding `1, 2, 1` | `{1, 2}` with `discarded: [order, multiplicity]` after three visits; for `2` against `[1]`, `collection.member-walk` (2) and `collection.member-test` (`p = 1`); for the second `1` against `[1, 2]`, `collection.member-walk` (2) and `collection.member-test` (`p = 1`) against `1`, equal, stopping; `collection.bound`; and a retain of `occ = 3`: eleven work units and three result units |
| Q10 | With TC-189 C06's `Holder`, `h1`, `h2` (identity bytes of `h1` before `h2`) and parameter `hs` of `Set<Holder>[0,2]` holding `h2, h1`: `contains(hs, h2)`; `contains(hs, h1)`; `contains(hs, h2)` under `ScalarLimitsV1` with every counter unlimited except `work_units: 3`; then `size(hs)` | true after `collection.member-walk` (4) and `collection.member-test` (`p = 2`) against `h1`, unequal, then the same two charges against `h2`, equal, then a scalar retain: thirteen work units and one result unit; true after one walk (4), one test (2) and a scalar retain, stopping at the first member: seven work units; `incomplete { limit_kind: work_units, limit: 3, consumed: 0, next_charge: 4, charge_point: collection.member-walk }`; 2 after one scalar retain only |
| Q11 | With `type Total = Integer;`, `fold<Total>(acc, x in q: acc + x, identity: 0)` for parameter `q` of `Sequence<Integer>[0,2]` holding `1, 2`; then under `ScalarLimitsV1` with every counter unlimited except `work_units: 8` | 3 after, per occurrence, `collection.visit` and the step's `integer-arithmetic.operands`, `integer-arithmetic.arithmetic` and `integer-arithmetic.result-retain`, then an accumulator retain with `occ = 1`: nine work units and three result units; `incomplete { limit_kind: work_units, limit: 8, consumed: 8, next_charge: 1, charge_point: collection.result-retain }` with no accumulator exposed |
| Q12 | With `type Small = Int[0,3];`, direct kernel evaluation of `sum<Small>(x in q: x)` for parameter `q` of `Sequence<Int[0,3]>[0,2]` holding `1, 2`: first with `ScalarLimitsV1` unlimited except the exact bounds `work_units: 6`, `result_units: 2` and `integer_bits: 3`; then with each one less (`work_units: 5`, `result_units: 1`, `integer_bits: 2`) and the other two exact; then holding `2, 2`; then empty; then linking `function s using V(q: Sequence<Int[0,3]>[0,2]): Small pure { sum<Small>(x in q: x) }` | 3 after `collection.visit` and seed `1` (no addition), `collection.visit`, `integer-arithmetic.operands` (`integer_bits = 2`, `value_occurrences = 2`), `integer-arithmetic.arithmetic` (`integer_bits = 3`), the uncharged decision `3` in `[0,3]`, `integer-arithmetic.result-retain` and a scalar `collection.result-retain`: six work units, two result units and `integer_bits` high-water 3; `incomplete { limit_kind: work_units, limit: 5, consumed: 5, next_charge: 1, charge_point: collection.result-retain }`; `incomplete { limit_kind: result_units, limit: 1, consumed: 1, next_charge: 1, charge_point: collection.result-retain }`; `incomplete { limit_kind: integer_bits, limit: 2, consumed: 2, next_charge: 3, charge_point: integer-arithmetic.arithmetic }`; a located undefined outcome at the addition after `integer-arithmetic.arithmetic` because `4` is outside `[0,3]`, with four work units, no result unit and no total; `0` after one scalar retain only (one work unit, one result unit, no addition); `refused { code: undefined_expression, cause: unproved-range }` at the `sum`, because the prefix `2 + 2` is not proved inside `Small` |

## Expected Results

Every positive and boundary result matches FR-145; each invalid, unsupported or exhausted mutation returns its exact typed disposition with source/provenance, emits no approximation, and preserves independent sibling results.
