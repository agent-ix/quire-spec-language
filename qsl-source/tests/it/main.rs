// SPDX-License-Identifier: AGPL-3.0-or-later
//! Single integration-test binary for `qsl-source`, following the same `it`
//! pattern as the root crate (`tests/it/main.rs`) and `qsl-cst`. Built only
//! with the `quire-extraction` feature (`required-features` in Cargo.toml).

mod quire_source;
